<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Administration_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="VDSM_hooks_defining_custom_properties" remap="TID_8291">
	<title>Defining Custom Properties</title>
	<para>
		The custom properties that are accepted by the Red Hat Virtualization Manager - and in turn passed to custom hooks - are defined using the <command>engine-config</command> command. Run this command as the <systemitem class="username">root</systemitem> user on the host where Red Hat Virtualization Manager is installed.
	</para>
	<para>
		The <literal>UserDefinedVMProperties</literal> and <literal>CustomDeviceProperties</literal> configuration keys are used to store the names of the custom properties supported. Regular expressions defining the valid values for each named custom property are also contained in these configuration keys.
	</para>
	<para>
		Multiple custom properties are separated by a semi-colon. Note that when setting the configuration key, any existing value it contained is overwritten. When combining new and existing custom properties, all of the custom properties in the command used to set the key's value must be included.
	</para>
	<para>
		Once the configuration key has been updated, the <systemitem>ovirt-engine</systemitem> service must be restarted for the new values to take effect.
	</para>
	<example>
		<title>Virtual Machine Properties - Defining the <parameter>smartcard</parameter> Custom Property</title>
		<orderedlist>
			<listitem>
				<para>
					Check the existing custom properties defined by the <literal>UserDefinedVMProperties</literal> configuration key using the following command: 
					<screen># engine-config -g UserDefinedVMProperties</screen>
				</para>
				<para>
					As shown by the output below, the custom property <parameter>memory</parameter> is already defined. The regular expression <literal>^[0-9]+$</literal> ensures that the custom property will only ever contain numeric characters.
				</para>
				<screen># engine-config -g UserDefinedVMProperties
UserDefinedVMProperties:  version: 3.6
UserDefinedVMProperties:  version: 4.0
UserDefinedVMProperties : memory=^[0-9]+$ version: 4.0</screen>
			</listitem>
			<listitem>
				<para>
					Because the <parameter>memory</parameter> custom property is already defined in the <literal>UserDefinedVMProperties</literal> configuration key, the new custom property must be appended to it. The additional custom property, <parameter>smartcard</parameter>, is added to the configuration key's value. The new custom property is able to hold a value of <literal>true</literal> or <literal>false</literal>.
				</para>
				<screen># engine-config -s UserDefinedVMProperties='memory=^[0-9]+$;smartcard=^(true|false)$' --cver=4.0</screen>
			</listitem>
			<listitem>
				<para>
					Verify that the custom properties defined by the <literal>UserDefinedVMProperties</literal> configuration key have been updated correctly.
				</para>
				<screen># engine-config -g UserDefinedVMProperties
UserDefinedVMProperties:  version: 3.6
UserDefinedVMProperties:  version: 4.0
UserDefinedVMProperties : memory=^[0-9]+$;smartcard=^(true|false)$ version: 4.0</screen>
			</listitem>
			<listitem>
				<para>
					Finally, the <systemitem>ovirt-engine</systemitem> service must be restarted for the configuration change to take effect.
				</para>
				<screen># systemctl restart ovirt-engine.service</screen>
			</listitem>
		</orderedlist>
	</example>
	<example>
		<title>Device Properties - Defining the <parameter>interface</parameter> Custom Property</title>
		<orderedlist>
			<listitem>
				<para>
					Check the existing custom properties defined by the <literal>CustomDeviceProperties</literal> configuration key using the following command: 
					<screen># engine-config -g CustomDeviceProperties</screen>
				</para>
				<para>
					As shown by the output below, no custom properties have yet been defined.
				</para>
				<screen># engine-config -g CustomDeviceProperties
CustomDeviceProperties:  version: 3.6
CustomDeviceProperties:  version: 4.0</screen>
			</listitem>
			<listitem>
				<para>
					The <literal>interface</literal> custom property does not already exist, so it can be appended as is. In this example, the value of the <literal>speed</literal> sub-property is set to a range of 0 to 99999, and the value of the <literal>duplex</literal> sub-property is set to a selection of either <literal>full</literal> or <literal>half</literal>.
				</para>
				<screen># engine-config -s CustomDeviceProperties="{type=interface;prop={speed=^([0-9]{1,5})$;duplex=^(full|half)$}}" --cver=4.0</screen>
			</listitem>
			<listitem>
				<para>
					Verify that the custom properties defined by the <literal>CustomDeviceProperties</literal> configuration key have been updated correctly.
				</para>
				<screen># engine-config -g CustomDeviceProperties
UserDefinedVMProperties:  version: 3.6
UserDefinedVMProperties:  version: 4.0
UserDefinedVMProperties : {type=interface;prop={speed=^([0-9]{1,5})$;duplex=^(full|half)$}} version: 4.0</screen>
			</listitem>
			<listitem>
				<para>
					Finally, the <systemitem>ovirt-engine</systemitem> service must be restarted for the configuration change to take effect.
				</para>
				<screen># systemctl restart ovirt-engine.service</screen>
			</listitem>
		</orderedlist>
	</example>
</section>
