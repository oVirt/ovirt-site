<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Administration_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="Kernel_Settings_Explained">
<title>Kernel Settings Explained</title>
	<para>
		The <guilabel>Kernel</guilabel> settings table details the information required on the <guilabel>Kernel</guilabel> tab of the <guilabel>New Host</guilabel> or <guilabel>Edit Host</guilabel> window. Common kernel boot parameter options are listed as check boxes so you can easily select them. For more complex changes, use the free text entry field next to <guilabel>Kernel command line</guilabel> to add in any additional parameters required.</para>
 <important>
  <para>
  If the host is attached to the Manager already, ensure you place the host into maintenance mode before applying any changes. You will need to reinstall the host by clicking <guibutton>Reinstall</guibutton>, and then reboot the host after the reinstallation is complete for the changes to take effect.
  </para>
 </important>
<table frame="all">
			<title><guilabel>Kernel</guilabel> Settings</title>
			<tgroup align="left" cols="2" colsep="1" rowsep="1">
				<colspec colname="c1" />
				<colspec colname="c2" />
				<thead>
					<row>
						<entry>
							<para>
								Field Name
							</para>
						</entry>
						<entry>
							<para>
								Description
							</para>
						</entry>
					</row>
				</thead>
				<tbody>
					<row>
						<entry>
							<para> 
							<guilabel>Hostdev Passthrough &amp; SR-IOV</guilabel>
							</para>
						</entry>
						<entry>
										<para>
											 Enables the IOMMU flag in the kernel to allow a host device to be used by a virtual machine as if the device is a device attached directly to the virtual machine itself. The host hardware and firmware must also support IOMMU. The virtualization extension and IOMMU extension must be enabled on the hardware. See <ulink url="https://access.redhat.com/documentation/en/red-hat-virtualization/4.0/paged/installation-guide/appendix-g-configuring-a-hypervisor-host-for-pci-passthrough">Configuring a Host for PCI Passthrough</ulink> in the <citetitle>Installation Guide</citetitle>. IBM POWER8 has IOMMU enabled by default. 
										</para>
             </entry>
           </row>
            <row>
						<entry>
							<para> 
							<guilabel>Nested Virtualization</guilabel>
							</para>
						</entry>
						<entry>
							<para>
             Enables the vmx or svm flag to allow you to run virtual machines within virtual machines. This option is only intended for evaluation purposes and not supported for production purposes. The <literal>vdsm-hook-nestedvt</literal> hook must be installed on the host.
							</para>
						</entry>
					</row>
           <row>
						<entry>
							<para> 
							<guilabel>Unsafe Interrupts</guilabel> 
							</para>
						</entry>
						<entry>
							<para>
              If IOMMU is enabled but the passthrough fails because the hardware does not support interrupt remapping, you can consider enabling this option. Note that you should only enable this option if the virtual machines on the host are trusted; having the option enabled potentially exposes the host to MSI attacks from the virtual machines. This option is only intended to be used as a workaround when using uncertified hardware for evaluation purposes.
							</para>
						</entry>
					</row>
          <row>
						<entry>
							<para> 
									<guilabel>PCI Reallocation</guilabel>
							</para>
						</entry>
						<entry>
							<para>
               If your SR-IOV NIC is unable to allocate virtual functions because of memory issues, consider enabling this option. The host hardware and firmware must also support PCI reallocation. This option is only intended to be used as a workaround when using uncertified hardware for evaluation purposes.
							</para>
						</entry>
					</row>
 	         <row>
						<entry>
							<para> 
								<guilabel>Kernel command line</guilabel>
							</para>
						</entry>
						<entry>
							<para>
               This field allows you to append more kernel parameters to the default parameters.
							</para>
						</entry>
					</row>
				</tbody>
			</tgroup>
		</table>
<note><para>If the kernel boot parameters are grayed out, click the <guibutton>reset</guibutton> button and the options will be available.</para></note>
</section>
