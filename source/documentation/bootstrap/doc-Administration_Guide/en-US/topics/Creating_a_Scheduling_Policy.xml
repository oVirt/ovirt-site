<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Administration_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="Creating_a_Scheduling_Policy" remap="TID_40829">
	<title>Creating a Scheduling Policy</title>
	<para>
		You can create new scheduling policies to control the logic by which virtual machines are distributed amongst a given cluster in your Red Hat Virtualization environment.
	</para>
	<procedure>
		<title>Creating a Scheduling Policy</title>
		<step>
			<para>
				Click the <guibutton>Configure</guibutton> button in the header bar of the Administration Portal to open the <guilabel>Configure</guilabel> window.
			</para>
		</step>
		<step>
			<para>
				Click <guilabel>Scheduling Policies</guilabel> to view the scheduling policies tab.
			</para>
		</step>
		<step>
			<para>
				Click <guibutton>New</guibutton> to open the <guilabel>New Scheduling Policy</guilabel> window.
			</para>
			<figure>
				<title>The New Scheduling Policy Window</title>
				<mediaobject>
					<imageobject>
						<imagedata fileref="images/7332.png" />
					</imageobject>
					<textobject> 
						<phrase>The New Scheduling Policy Window</phrase>
					</textobject>
				</mediaobject>
			</figure>
		</step>
		<step>
			<para>
				Enter a <guilabel>Name</guilabel> and <guilabel>Description</guilabel> for the scheduling policy.
			</para>
		</step>
		<step>
			<para>
				Configure filter modules:
			</para>
			<substeps>
				<step>
					<para>
						In the <guilabel>Filter Modules</guilabel> section, drag and drop the preferred filter modules to apply to the scheduling policy from the <guilabel>Disabled Filters</guilabel> section into the <guilabel>Enabled Filters</guilabel> section.
					</para>
				</step>
				<step>
					<para>
						Specific filter modules can also be set as the <guilabel>First</guilabel>, to be given highest priority, or <guilabel>Last</guilabel>, to be given lowest priority, for basic optimization.
					</para>
					<para>
						To set the priority, right-click any filter module, hover the cursor over <guilabel>Position</guilabel> and select <guilabel>First</guilabel> or <guilabel>Last</guilabel>.
					</para>
				</step>
			</substeps>
		</step>
		<step>
			<para>
				Configure weight modules:
			</para>
			<substeps>
				<step>
					<para>
						In the <guilabel>Weights Modules</guilabel> section, drag and drop the preferred weights modules to apply to the scheduling policy from the <guilabel>Disabled Weights</guilabel> section into the <guilabel>Enabled Weights &amp; Factors</guilabel> section.
					</para>
				</step>
				<step>
					<para>
						Use the <guibutton>+</guibutton> and <guibutton>-</guibutton> buttons to the left of the enabled weight modules to increase or decrease the weight of those modules.
					</para>
				</step>
			</substeps>
		</step>
		<step>
			<para>
				Specify a load balancing policy:
			</para>
			<substeps>
				<step>
					<para>
						From the drop-down menu in the <guilabel>Load Balancer</guilabel> section, select the load balancing policy to apply to the scheduling policy.
					</para>
				</step>
				<step>
					<para>
						From the drop-down menu in the <guilabel>Properties</guilabel> section, select a load balancing property to apply to the scheduling policy and use the text field to the right of that property to specify a value.
					</para>
				</step>
				<step>
					<para>
						Use the <guibutton>+</guibutton> and <guibutton>-</guibutton> buttons to add or remove additional properties.
					</para>
				</step>
			</substeps>
		</step>
		<step>
			<para>
				Click <guibutton>OK</guibutton>.
			</para>
		</step>
	</procedure>
</section>

