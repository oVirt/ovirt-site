<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE appendix PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "Administration_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="How_to_Set_Up_RHVM_to_Use_FCoE">
 <title>How to Set Up Red Hat Virtualization Manager to Use FCoE</title>
    <para>You can configure Fibre Channel over Ethernet (FCoE) properties for host network interface cards from the Administration Portal. The <guimenuitem>fcoe</guimenuitem> key is not available by default and needs to be added to the Manager using the engine configuration tool. You can check whether <guimenuitem>fcoe</guimenuitem> has already been enabled by running the following command:</para>
    <screen># engine-config -g UserDefinedNetworkCustomProperties</screen>
    <para>You also need to install the required VDSM hook package on the hosts. Depending on the FCoE card on the hosts, special configuration may also be needed; see <ulink url="https://access.redhat.com/documentation/en-US/Red_Hat_Enterprise_Linux/7/html/Storage_Administration_Guide/fcoe-config.html">Configuring a Fibre Channel over Ethernet Interface</ulink> in the <citetitle>Red Hat Enterprise Linux Storage Administration Guide</citetitle>.</para>
  <procedure>
  	<title>Adding the fcoe Key to the Manager</title>
  	  <step>
  	  	<para>
  	  		On the Manager, run the following command to add the key:
  	  	</para>
  	  	<screen># engine-config -s UserDefinedNetworkCustomProperties='fcoe=^((enable|dcb|auto_vlan)=(yes|no),?)*$'
</screen>
  	  </step>
  	  <step>
  	  	<para>
  	  		Restart the <command>ovirt-engine</command> service: 
  	  	</para>
  	  	<screen># systemctl restart ovirt-engine.service</screen>
  	  </step>
      <step>
        <para>Install the VDSM hook package on each of the Red Hat Enterprise Linux hosts on which you want to configure FCoE properties. The package is available by default on Red Hat Virtualization Host (RHVH).</para>
        <screen># yum install vdsm-hook-fcoe</screen>
      </step>
  </procedure>	  
  <para>The <guimenuitem>fcoe</guimenuitem> key is now available in the Administration Portal. See <xref linkend="Editing_host_network_interfaces"/> to apply FCoE properties to logical networks.</para>
</section>